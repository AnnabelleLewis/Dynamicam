class_name CameraConstraint

# Handles camera constraints

#   Takes in the camera position and rotation, returns a dictionary with the constrained variables
# Possible dict vars are position, rotation, and zoom
func _apply_constraint(position,rotation,zoom) -> Dictionary:
	return {}
